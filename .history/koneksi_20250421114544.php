<!-- <?php
$host = "localhost";
$username = "root";
$password = "";
$database = "todolist";

$conn = new mysqli($host, $username, $password, $database);

if ($conn->connect_error) {
    die("Koneksi database gagal: " . $conn->connect_error);
} else {
    echo "Koneksi database berhasil!"; // Tambahkan baris ini untuk tes
}
?> -->